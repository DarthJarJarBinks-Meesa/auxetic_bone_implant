# --- GOOGLE COLAB SETUP FOR AUXETIC BONE IMPLANT ---
# Run these cells in order to set up the environment properly.

# 1. Install condacolab to handle complex CadQuery dependencies
!pip install -q condacolab
import condacolab
condacolab.install()

# --- AFTER THE KERNEL RESTARTS AUTOMATICALLY, RUN THIS CELL ---

# 2. Install CadQuery and system dependencies via Conda
!conda install -c conda-forge cadquery gmsh calculix-ccx -y

# 3. Clone the repository (if you haven't already or need a fresh copy)
!git clone https://github.com/DarthJarJarBinks-Meesa/auxetic_bone_implant.git
%cd auxetic_bone_implant

# 4. Install Python requirements
!pip install -r requirements.txt

# 5. Run a small test sweep (5 cases) to verify everything works
!python src/main.py --mode baseline_only --stop-on-first-failure

# 6. If (5) passes, run the full sweep!
# !python src/main.py --mode full_factorial
